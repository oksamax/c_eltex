Библиотека должна состоять из объектых модулей, функции которых принимают два параметра типа float, а также возвращают результат функции типа float.

Example:

float add(float a, float b)
{
	return (a + b);
}
